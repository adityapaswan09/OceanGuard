const vessels = [{ name: "Ocean Pioneer", type: "Tanker", flag: "IN", score: 84 }, { name: "Arabian Trader", type: "Cargo", flag: "PA", score: 71 }, { name: "Meridian Dawn", type: "Product tanker", flag: "SG", score: 56 }];

export function VesselWatchlist() {
    return <section className="border-t border-line bg-white px-5 py-4 lg:px-6"><div className="mb-3 flex items-center justify-between"><div><p className="eyebrow text-signal">Monitored vessels</p><p className="mt-1 text-[11px] text-mist">Latest AIS contacts in region</p></div><span className="font-mono text-[10px] text-mist">03 TRACKS</span></div><div className="grid gap-2 md:grid-cols-3">{vessels.map((vessel, index) => <div className="flex items-center gap-3 rounded-sm border border-line px-3 py-2.5" key={vessel.name}><span className="font-mono text-[10px] text-mist">0{index + 1}</span><div className="min-w-0 flex-1"><p className="truncate text-[11px] font-semibold text-[#173247]">{vessel.name}</p><p className="mt-1 text-[9px] text-mist">{vessel.type} · {vessel.flag}</p></div><div className="text-right"><p className="font-display text-lg text-signal">{vessel.score}</p><p className="text-[8px] uppercase tracking-[.1em] text-mist">score</p></div></div>)}</div></section>;
}
